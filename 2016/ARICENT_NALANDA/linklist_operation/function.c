#include<header.h>
list_t *addEnd(list_t *start)
{
	list_t *temp;
	list_t *temp = head->next;
	if (NULL == temp)
	{
		temp = malloc(sizeof(list_t));
		
	}
	while (temp)
	{
		temp = temp->next;
	}
	
	
}
