#include<stdio.h>
#include "add.h"
#define ADD(A,B) (A + B) //using macros

//defining main function
int main()
{
  int a = 20;
  int b = 30;
  printf("Addition of A and B :%d\n",ADD(a,b));
  return 0;
}

